name = "quality-of-life-sharing"
compatibility_date = "2026-07-11"

# Serve the built Slidev SPA from the dist folder
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
html_handling = "auto-trailing-slash"

# Optional: add a custom domain in Cloudflare Dashboard:
# Workers & Pages → quality-of-life-sharing → Settings → Triggers → Custom Domains
