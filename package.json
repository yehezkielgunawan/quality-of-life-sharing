{
  "name": "quality-of-life-sharing",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "cloudflare:build:ci": "slidev build --base /",
    "dev": "slidev --open",
    "export": "slidev export",
    "deploy:cf": "slidev build && rm -f dist/_redirects && npx wrangler deploy"
  },
  "dependencies": {
    "@slidev/cli": "^52.17.0",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "latest",
    "vue": "^3.5.33"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
